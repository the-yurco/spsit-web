import React from 'react';

type Props = {};

const page = (props: Props) => {
	return (
		<div>
			toto je to iste co aj pri O skole == vedenie skoly... cize toto len bude
			prelikovanie ku tomu hore
		</div>
	);
};

export default page;
